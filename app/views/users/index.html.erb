<h1>Alle Benutzer</h1>

<table>
  <tr>
    <th>ID</th>
    <th>Benutzername</th>
  </tr>

  <% @users.each do |user| %>
  <tr>
    <td><%= user.id %></td>
    <td><%= link_to user.name, user %></td>
  </tr>
  <tr>
    <td><%= link_to 'Show', user %></td>
  </tr>
<% end %>
</table>

<br />
