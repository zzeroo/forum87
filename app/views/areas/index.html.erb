<h1>Alle Bereiche</h1>
<%= link_to "Home", root_path %>

<table class="normal">
  <tr>
    <th>ID</th>
    <th>Name</th>
    <th></th>
  </tr>

  <% @areas.each do |area| %>
  <tr>
    <td><%= area.id %></td>
    <td><%= area.name %></td>
    <td><%= link_to 'Show', area %></td>
  </tr>
<% end %>
</table>

<br />
