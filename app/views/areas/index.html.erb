<h1>Alle Bereiche</h1>
<ul class="menu">
  <li><%= link_to "Home", root_path %></li>
</ul>

<table class="normal">
  <tr>
    <th>ID</th>
    <th>Name</th>
    <th></th>
  </tr>

  <% @areas.each do |area| %>
  <tr>
    <td><%= area.id %></td>
    <td><%= area.name %></td>
    <td><%= link_to 'Show', area %></td>
  </tr>
<% end %>
</table>

<br />
