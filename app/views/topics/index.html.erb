<h1>Alle Themen</h1>
<ul class="menu">
  <li><%= link_to "Home", root_path %></li>
</ul>

<table class="normal">
  <tr>
    <th>ID</th>
    <th>&Uuml;berschrift</th>
    <th>Untertitel</th>
    <th></th>
  </tr>

  <% @topics.each do |topic| %>
  <tr>
    <td><%= topic.id %></td>
    <td><%= link_to topic.headline, topic %></td>
    <td><%= topic.subtitle %></td>
    <td><%= link_to 'Show', topic %></td>
  </tr>
<% end %>
</table>

<br />
